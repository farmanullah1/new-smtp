<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Farmanullah Company SMTP &amp; Authentication Suite - Production-ready Handlebars SSR platform with dynamic templates, full auth lifecycle, and CRUD resource management.">
  <title>{{#if pageTitle}}{{pageTitle}} — {{/if}}Farmanullah Company SMTP &amp; Auth Suite</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Plus+Jakarta+Sans:wght@700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/app.css">
</head>
<body>
  {{#if currentUser}}
    {{> navbar}}
    {{> mobile-nav}}
  {{/if}}

  {{> flash}}

  <!-- Main Body Content -->
  <main class="page-container animate-fade-in" role="main">
    {{{body}}}
  </main>

  {{> footer}}

  <script src="/js/app.js"></script>
</body>
</html>
